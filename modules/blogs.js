const mongoose = require("mongoose");
// To create a schema and a model for the data
// schema defines the structure of the documents that we gonna store inside the collection
// mongoose.Schema here is actually a constructor function
const Schema = mongoose.Schema;